PORT=4000
JWT_SECRET=change_this_to_a_long_random_string

# Optional future LLM rewrite provider. Leave empty to use the built-in
# deterministic rewrite engine, which only rewrites wording from existing resume facts.
AI_API_KEY=
AI_MODEL=
