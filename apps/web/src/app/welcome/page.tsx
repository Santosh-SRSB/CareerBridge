import { CandidateWelcomeLanding } from '@/components/landing/CandidateWelcomeLanding';

/** Public candidate welcome — distinct UI from the employer landing. */
export default function CandidateWelcomePage() {
  return <CandidateWelcomeLanding />;
}
