import { BrandSplash } from "@/components/landing/AppLoader";

export default function Loading() {
  return (
    <div className="app-loader" role="status" aria-label="Loading CareerBridge">
      <BrandSplash />
    </div>
  );
}
